export interface Stage {
  etapa: string;
  idUnico: string;
  descripcion: string;
  fechaInicio: string;
  fechaFinal: string;
  fechaFinalizacion: string;
  porcentaje: number;
  dependencia: string;
}